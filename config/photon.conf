# Deep power-off via &so on the control layer; wake by pressing any key
# (kscan0 has wakeup-source). Peripheral has CONFIG_ZMK_SLEEP=n, so this
# is the only way to power down without pulling the battery.
CONFIG_ZMK_PM_SOFT_OFF=y
